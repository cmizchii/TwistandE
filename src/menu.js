export  function createMenu() {
  const content = document.getElementById('content');

  const text = document.createElement('h1');
  text.textContent = 'menu page (coming soon)';

  content.appendChild(text);
}